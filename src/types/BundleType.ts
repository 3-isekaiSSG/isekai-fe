export interface BundleIdType {
  id: number
  bundleId: number
  code: number
}

export interface BundleCardType {
  bundleId: number
  outerName: string
  code: number
  minPrice: number
  buyCount: number
  imgUrl: string
}
